<Window x:Class="Isotop2.Forms.AddRadiationExposureToOrganForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Isotop2.Forms"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Добавление" Height="140" Width="440" 
        Icon="/Resources/add.jpg"
        Loaded="EditRadiationExposureToOrganForm_Load"
        KeyDown="PressHotKey">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="60"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="130"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="120"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="ItemsControl.HorizontalAlignment" Value="Left"/>
            <Setter Property="ItemsControl.VerticalAlignment" Value="Top"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Name="label_Marker" Content="Маркер" Margin="5,5,0,0"/>
        <Label Grid.Column="1" Name="label_Organ" Content="Орган" Margin="5,5,0,0"/>
        <Label Grid.Column="2" Name="label_Coefficient" Content="Коэффицент" Margin="5,5,0,0"/>
        <ComboBox Grid.Column="0"  Name="comboBox_Marker" Margin="10,30,0,0"/>
        <ComboBox Grid.Column="1" Name="comboBox_Organ" Margin="10,30,0,0"/>
        <TextBox Grid.Column="3" Name="textBox_Coefficient" PreviewTextInput="NumberValidationTextBox" Margin="0,32,10,0"/>
        <Button Grid.Column="2" Name="button_OK" Content="Добавить" Click="button_OK_Click" Margin="0,65,80,10"/>
        <Button Grid.Column="2" Name="button_Cancel" Content="Отмена" Click="button_Cancel_Click" Margin="0,65,10,10"/>
    </Grid>
</Window>
